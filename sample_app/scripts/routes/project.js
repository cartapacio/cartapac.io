/*global define*/

define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var ProjectRouter = Backbone.Router.extend({
        routes: {
        }

    });

    return ProjectRouter;
});
